import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hu.mindsol.ionic.first',
  appName: 'ionicfirst',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
